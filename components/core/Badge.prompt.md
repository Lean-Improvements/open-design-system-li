Tiny uppercase mono label for statuses, counts and tags.

```jsx
<Badge>In progress</Badge>
<Badge variant="secondary">Draft</Badge>
<Badge variant="destructive">Blocked</Badge>
```

Variants: `default`, `secondary`, `destructive`, `outline`. Always renders in JetBrains Mono, uppercase, 4px radius.
