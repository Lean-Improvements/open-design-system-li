Standard text field. Pairs with a `Label` above it.

```jsx
<Input placeholder="Search epics…" />
<Input type="email" defaultValue="hola@leanimprovements.es" />
```

40px tall, 8px radius, hairline border. Focus tints the border to Cursor Orange. Always give it an associated label for a11y.
